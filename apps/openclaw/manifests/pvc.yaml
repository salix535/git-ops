apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-home-pvc
  labels:
    app: openclaw
spec:
  storageClassName: longhorn-strict-local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
