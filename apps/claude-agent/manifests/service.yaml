apiVersion: v1
kind: Service
metadata:
  name: claude-agent
  labels:
    app: claude-agent
spec:
  type: ClusterIP
  selector:
    app: claude-agent
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
