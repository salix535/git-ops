apiVersion: v1
kind: Service
metadata:
  name: n8n
  labels:
    app: n8n
  annotations:
    metallb.universe.tf/loadBalancerIPs: 10.0.4.151
spec:
  type: LoadBalancer
  selector:
    app: n8n
  ports:
    - name: http
      port: 80
      targetPort: 5678
      protocol: TCP
